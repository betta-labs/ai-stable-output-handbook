# 附录G AI Failure Casebook（故障与失败案例集）

本附录是一个**持续积累的活资产（Postmortem Database）**。

与正文第 15 章相对完整、宏观的案例研究不同，本案例集聚焦于微观的、技术或流程上的**具体故障模式（Failure Modes）**。每个案例均使用标准的故障分析格式，标明诊断层面、失败信号、回退过程及根本教训，旨在帮助团队在遭遇类似问题时能"对号入座"、快速自诊。

---

## 案例一：消失的条件约束（上下文债务导致的指令漂移）

- **诊断层面**：数据与知识层 / 任务定义层
- **适用场景**：高频、多规则文本生成（如合同合规审查、敏感公文起草）
- **故障现象**：系统稳定运行 2 个月后，突然开始忽略"禁止透露客户手机号"、"禁止使用特定缩写"等核心安全红线。

### 1. 失败信号（Symptoms）
- 人工验证环节拦截到包含敏感信息的输出
- 一次通过率在未修改核心提示词的情况下，从 90% 降至 60%
- 模型输出中出现不符合最新合规要求的陈述

### 2. 故障根因（Root Cause Analysis）
- **上下文债务积累**：业务规则不断变更，团队为了"打补丁"，在提示词末尾和知识库中堆积了越来越多的例外规则。
- **注意力稀释（Attention Loss）**：提示词总长度超过 4000 字。由于长上下文的"中间丢失"（Lost in the Middle）效应，模型对位于中段的"安全禁止性规则"注意力大幅下降，导致指令失效。

### 3. 回退过程（Rollback & Mitigation）
- **步骤1**：紧急下线当前的提示词版本，回滚至 30 天前的稳定版本。
- **步骤2**：将需要保护的安全规则移至**最前端的系统提示词（System Prompt）**中。
- **步骤3**：对长提示词进行重构，精简掉 40% 的冗余背景信息，使规则之间互不冲突。

### 4. 根本教训（Key Takeaways）
- 提示词不是无限容量的垃圾桶，规则每增加一条，旧规则的约束力就下降一分。
- 必须定期（每季度）进行**提示词重构（Refactoring）**。
- 将禁止性规则与内容生成逻辑物理分离：一阶段模型只负责生成，二阶段轻量模型（或正则表达式）负责专项合规拦截。

---

## 案例二：被迫下线的"明星客服"（过度微调导致的通用能力丧失）

- **诊断层面**：认知层 / 进阶技术选型
- **适用场景**：客户服务、专业咨询智能体
- **故障现象**：微调后的客服模型在回答特定业务问题时话术极其完美，但一旦用户换个问法，或询问周边产品，模型就会开始胡言乱语。

### 1. 失败信号（Symptoms）
- 在标准测试集之外的真实问答中，客户投诉率飙升
- 模型在回答"非训练集内"问题时，出现严重的幻觉
- 对简单的日常打招呼或闲聊，模型也强行套用复杂的业务话术

### 2. 故障根因（Root Cause Analysis）
- **灾难性遗忘（Catastrophic Forgetting）**：微调时使用的示例数据虽然极其专业，但数据分布过于单一。训练过程中，模型的基础常识和通用理解能力的权重被强行扭曲覆盖。
- **过拟合（Overfitting）**：过度微调使得模型失去了泛化（Generalization）能力，变成了只能回答特定 50 个问题的"复读机"。

### 3. 回退过程（Rollback & Mitigation）
- **步骤1**：立即切换回"基础模型 + RAG 检索"的 L3 级人工看守模式。
- **步骤2**：重新设计微调方案，在训练数据中按 3:1 的比例混入通用常识对话数据（Data Mixing），防止模型失忆。

### 4. 根本教训（Key Takeaways）
- 不要试图用微调解决"知识量不够"的问题，微调只用来规范"语气和特定格式输出"。
- 业务周边知识的覆盖，应该由 RAG 在检索层提供，而不是写进模型参数。
- 每次微调完成后，必须进行通用能力评估测试（General Ability Benchmarking），确保基础能力没有退化。

---

## 案例三：爆单后的巨额账单（死循环熔断机制缺失）

- **诊断层面**：自动化/Agent 决策层
- **适用场景**：多步循环自主 Agent（如自动化信息检索、多轮对话验证）
- **故障现象**：公司在一场营销活动期间爆单，随之而来的是当月 API 账单激增 15 倍，其中单次任务的调用成本居然达到了上百美元。

### 1. 失败信号（Symptoms）
- 财务后台监控到 API Key 消费异常报警
- 多个并发任务执行超时，下游系统拥堵
- 审计日志中出现单个任务对 LLM 的连续数百次相同调用

### 2. 故障根因（Root Cause Analysis）
- **Agent 规划死循环（Loop Atrophy）**：Agent 在执行"查找某产品底价并填入表格"任务时，由于该产品在数据库中信息缺失，API 返回空值。Agent 逻辑是"没查到就继续查"，且提示词没有给出"查不到时平滑退出的路径"。
- **未设物理限额**：技术团队直接将 API Key 的无限额版本硬编码给 Agent，且系统后台没有限制单个任务的最大 Loop 次数。

### 3. 回退过程（Rollback & Mitigation）
- **步骤1**：在网关后台紧急停用该 Agent 的 API 授权，终止所有运行中的进程。
- **步骤2**：在代码和提示词中强制加入 **`max_loops = 5`** 物理熔断限制，超过 5 次循环直接报错并呼叫人工。
- **步骤3**：对 Agent 进行重构，加入"如果查不到，将该单元格标记为【信息缺失】并结束该步骤"的平滑退出分支。

### 4. 根本教训（Key Takeaways）
- 永远不要信任 Agent 能自己走出死循环。
- 在任何自主 Agent 或自动化流程中，**必须限制最大循环次数（熔断器）**和单次任务的最大 Token 预算。
- 凡是可能返回空值或报错的外部 API 调用，必须有显式的异常处理逻辑（Exception Handling）。

---

## 社区贡献与案例征集

Failure Casebook 是一个持续成长的活资产。我们深知，最真实的教训往往隐藏在一线团队的"翻车事故"中。

如果你在实际使用或推动 AI 落地的过程中，也遭遇了类似的稳定性故障，欢迎将你的案例整理并发送至：**jacejacejia@gmail.com**，格式如下：

```text
- 场景背景：(简述业务场景)
- 诊断层面：(参考第2章的诊断分类)
- 失败信号：(你当时看到了什么异常现象？)
- 根因分析：(技术或流程上的真实漏洞是什么？)
- 避坑/回退方法：(你们是怎么修复并防止其再次发生的？)
```

一经采纳，我们将会在后续版本中更新，并署名感谢。
