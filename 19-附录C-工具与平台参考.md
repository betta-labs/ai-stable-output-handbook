# 附录C 工具与平台参考

本附录为正文第 4 章、第 9 章和第 10 章提供工程落地层面的工具参考。由于 AI 工具链演进速度极快，本附录提供的是**通用分类与选型逻辑**，具体平台的最新功能与接口请参考其官方文档。

---

## 一、API 接入与网关中台（Model Gateway）

对于企业而言，直接让业务系统硬编码调用单一模型供应商（如直接调用 OpenAI API）是高风险的行为。建议在业务系统与模型之间，架设一层**统一大模型网关**：

### 1. 核心价值
- **多模型平滑切换**：主模型停服或额度耗尽时，网关可在1秒内将请求路由到备用模型
- **统一身份认证与计费**：企业内部多个系统共用一个网关，实现集中的Token消耗审计与预算控制
- **敏感数据拦截（DLP）**：在数据发送给外部模型前，自动识别并脱敏PII（个人敏感信息）
- **负载均衡与重试**：自动处理API超时和高并发限流（Rate Limit）

### 2. 开源及商业选型参考
- **One API / New API**：国内开源社区最流行的多源大模型转接中台，支持几乎所有中外主流模型的统一格式转换与配额管理。
- **Portkey / LiteLLM**：企业级大模型网关，提供完善的路由、缓存、重试、以及监控面板。
- **Azure OpenAI**：对于数据合规性要求极高的企业，通过微软Azure部署OpenAI模型可以确保数据不出企业专有网络，符合GDPR等合规要求。

---

## 二、工作流编排工具对比（Workflow Orchestration）

当任务复杂、需要多步处理或调用外部工具时，使用图形化或代码化的工作流编排工具，比直接在代码里写 `if-else` 要稳定和高效得多。

| 工具 | 适用定位 | 优势 | 劣势 |
|------|---------|-----|-----|
| **Dify** | 业务人员与开发者协同的首选平台 | 图形化工作流非常直观，开箱即用，内置了完善的知识库（RAG）和工具管理，支持 L4 级 Agent 搭建 | 复杂逻辑（如高度动态的循环）表达能力不如纯代码 |
| **Coze (扣子)** | 快速原型设计、低代码智能体 | 字节跳动出品，工具生态极丰富，插件一键调用，适合快速跑通 MVP | 深度定制和私有化部署受限 |
| **n8n / Make** | 通用自动化流水线（L2级） | 传统的自动化编排工具，对主流办公软件（邮件、Slack、Excel等）的连接支持极完美，可轻松嵌入大模型单步节点 | RAG 相关的专项支持不如 Dify 等原生 AI 工具 |
| **LangChain / LlamaIndex** | 程序员代码开发（L1-L3级） | 开发者生态最庞大，代码级控制度 100%，适合构建高度定制的底层 RAG 和多智能体系统 | 学习曲线陡峭，版本更新极其频繁，容易出现兼容性问题 |

---

## 三、知识库与 RAG 平台参考

RAG 系统的核心在于检索。以下是不同规模团队的选型建议：

### 1. 轻量级 RAG（1~3 周快速上线）
直接使用 **Dify** 或 **Coze** 内置的知识库功能。
- **优势**：自动处理文档分块、向量化（Vectorization）、和检索合并，无需写任何代码。
- **适用场景**：50 个文档以内，规则相对简单的业务场景。

### 2. 专业级 RAG（需要精细化检索控制）
使用专业的 RAG 工具（如 **MaxKB**、**Langgenius**）或直接使用向量数据库（Vector DB）。
- **向量数据库选型**：
  - **Milvus / Qdrant**：适合百万级以上文档的大规模企业检索。
  - **Pinecone / PGVector**：适合云原生或已有PostgreSQL数据库的团队。
- **混合检索（Hybrid Search）**：强烈建议开启"向量检索 + 关键词检索"的混合检索模式，这能大幅提升中文业务专有名词的检索准确率。

---

## 四、安全审核与防护工具

为了在设计阶段就处理掉安全合规风险（第14章），可以在工作流中引入安全防护层：

- **Llama Guard**：Meta 开源的模型安全看守，能够快速判断用户的输入或模型的输出是否包含违规、暴力、或敏感内容。
- **NeMo Guardrails**：NVIDIA 开源的模型防护框架，可以为 LLM 强行施加"行为轨道"和"事实核对"规则，防止其给出范围外的回答。
- **PII 脱敏过滤脚本**：在发送请求至 API 网关前，使用正则表达式或 SpaCy 等轻量级 NLP 工具，自动识别并屏蔽手机号、身份证、邮箱等敏感数据。

---

## 五、给团队的工具链演进建议

1. **第 1 个月（试点阶段）**：
   - 客户端：统一使用ChatGPT Team 或Claude Team官方界面（杜绝个人 Shadow AI）。
   - 编排：用 **Dify官方云服务**快速搭建 1~2 个最小闭环（L2~L3 级）。

2. **第 3 个月（团队推广阶段）**：
   - 网关：部署 **One API** 统一管理各模型 key。
   - 编排：在公司内部服务器私有化部署 **Dify**，将所有知识库文档本地化存储。

3. **第 6 个月以上（深度治理阶段）**：
   - 评测：引入 **LLM-as-Judge** 并在 Dify 后台自动记录所有一次通过率数据。
   - 韧性：建立 API 切换网关，随时准备在 OpenAI、Anthropic 和本地私有部署模型（如 Qwen2.5-72B）之间一键切换。
