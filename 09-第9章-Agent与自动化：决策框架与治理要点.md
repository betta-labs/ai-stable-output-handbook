# 第9章 Agent与自动化：决策框架与治理要点

随着大模型能力从”单次对话“演进到“自主规划”，许多企业和团队开始尝试搭建 Agent（智能体）或复杂的多步自动化工作流。

Agent 确实展现出了极高的能力上限：它们能够理解复杂的自然语言任务，拆解为多个步骤，自主选择并调用外部工具（如搜索、运行代码、读写数据库），根据每一步的返回结果动态调整下一步计划，直到达成目标。

然而，企业在将Agent引入真实业务时，往往会遭遇稳定性上的"滑铁卢"。第15章的"跨系统自动周报智能体"失败案例（由于多步链路的稳定性衰减导致一次通过率只有约53%）就是一个典型的警示。

**本章不讲"怎么写一个 Agent 系统的代码"，而是站在决策与治理的角度，回答两个核心问题：**
1. 我们的业务场景，到底该不该上 Agent？
2. 如果必须要上，怎么进行设计与治理，才能让它安全、可控、长期稳定？

---

## 一、自主性的代价：为什么Agent容易失控

在传统软件工程中，流程是高度确定的（If-Then-Else）。在大模型单任务工作流中，虽然输出有波动性，但由于步骤单一，可以通过严格的提示词模板和单步验证清单进行强力拦截。

但在Agent场景中，你让渡了**控制权**。Agent的核心特征是"多步自主规划"（Planning & Loop），而这种自主性带来了两个根本性的稳定性挑战：

### 1. 稳定性衰减的乘法效应（$P^n$ 衰减模型）

如果一个自动化流程包含 $n$ 个步骤，每一步骤的成功率（一次通过率）为 $P_i$，那么整个流程的最终成功率 $P_{total}$ 是每一步成功率的乘积：

$$P_{total} = P_1 	imes P_2 	imes P_3 	imes \dots 	imes P_n$$

假设你设计了一个包含 5 个步骤的自动化 Agent（1. 检索邮件 -> 2. 提取需求 -> 3. 查询库存数据库 -> 4. 生成回复初稿 -> 5. 自动发送邮件），即使每一步的成功率高达 90%（在 LLM 任务中这已经是非常优秀的水平）：

$$P_{total} = 0.9 	imes 0.9 	imes 0.9 	imes 0.9 	imes 0.9 \approx 59\%$$

这意味着，**将近一半的运行会以失败告终**。在真实的复杂业务中，步骤越多，最终成功率会呈指数级下降。

### 2. 错误积累与自我强化（Error Propagation）

在单步工作流中，如果第一步出错，流程通常会立即中断或报错。但在Agent自主循环中，如果第2步产生了一个微小的幻觉（例如混淆了两个客户的名称），Agent会将这个错误信息作为第3步的输入继续运行。

到了第4步，Agent会基于这个错误的上下文，做出完全扭曲的“自主决策”（例如认为应该给该客户顶格折扣），最终在第5步自动执行输出。这种错误在链路中被放大、变形且自我合理化的现象，被称为**错误传播**。

---

## 二、Agent适用条件决策树

在决定为某个场景开发Agent之前，必须通过以下决策树进行严格评估。**不要因为技术时髦而选择复杂方案。**

```text
开始评估：该不该用 Agent？
│
├── 1. 该场景的容错率如何？
│   ├── 极低（如资金划拨、对外正式公文、医疗决策、直接修改核心数据库） ──> ❌ 绝对禁止使用自主Agent
│   └── 中等或高（有容错空间，或有严格的人工审核环节拦截） ──> 进入下一步
│
├── 2. 任务步骤是否可以被固化为确定的If-Then流程？
│   ├── 是（步骤是固定的，不需要模型动态决定下一步干什么） ──> ❌ 使用“传统自动化(RPA/确定性工作流)”，稳定性最高
│   └── 否（步骤高度依赖输入内容，需要根据中间结果动态决定下一步） ──> 进入下一步
│
├── 3. 该任务需要的平均自主步骤数（Loop次数）是多少？
│   ├── 超过5步 ──> ⚠️ 稳定性风险极大，建议拆分为多个单步工作流，人机协同过渡
│   └── 5步以内 ──> 进入下一步
│
└── 4. 团队是否有能力为该Agent建设完整的评测集与监控熔断系统？
    ├── 否（只想写个提示词让它跑） ──> ❌ 暂缓，使用单任务工作流
    └── 是（有技术和管理资源进行长期治理） ──> ✅ 可以尝试搭建Agent
```

---

## 三、自主性的五个分级策略

不要把"全自动"当成唯一目标。根据场景的风险和技术成熟度，选择最合适的自主性级别：

| 级别 | 自主性程度 | 人机协作模式 | 适用场景 | 稳定性 |
|------|-----------|-------------|---------|-------|
| **L1：单步辅助** | 无自主性 | 人类手动执行每一步，AI 只作为单步输入/输出的润色或提取工具 | 复杂分析、敏感沟通、首版起草 | 极高（100% 受控） |
| **L2：半自动流水线** | 极低自主性 | 步骤固定的流水线，每步之间自动传递数据，但人类在关键节点审核 | 周报整理、标准格式转换、高频数据搬运 | 高（步骤受控，拦截点明确） |
| **L3：人机协同智能体** | 中等自主性 | Agent可以自主规划步骤、调用工具，但**每一步执行前**必须人类确认 | 竞品调研、标准合同条款初审、多源信息检索 | 中（执行受控，时间成本稍高） |
| **L4：授权后自主** | 高自主性 | Agent 完全自主执行全流程，但在**最终执行输出（如发送、写入）前**需人类一键确认 | 客户日常邮件自动草拟、标准周报生成、初级客服回复 | 中等偏低（最终关口有人看守） |
| **L5：全自主运行** | 完全自主 | Agent闭环运行，无人类干预，仅后台记录日志 | 低风险高频任务、内部监控报警、非公开测试场景 | 低（极易失控，需强力熔断机制） |

**企业落地铁律：** 在任何业务场景中，**初始上线必须从L2或L3开始**。连续4周一次通过率达到90%以上，且未发生任何安全合规事件，方可申请升级到 L4。对于涉及外部客户、财务、核心数据的场景，**严禁使用L5**。

---

## 四、Agent的稳定设计原则：控制与防线

如果你评估后决定采用L3或L4级别的Agent，设计时必须遵循以下三大"防线原则"，主动对对抗乘法衰减。

### 防线一：限制工具的"破坏力"（Least Privilege）

不要给Agent无限制的工具访问权限。
- **只读不写**：给Agent调用的 API 应该是只读的。例如，允许Agent查询库存和价格，但绝对不允许Agent直接调用"修改价格"或"直接下单"的写操作API。
- **沙箱隔离**：如果Agent需要运行代码（如数据分析智能体），必须在严格隔离的、有时间限制和资源限制的沙箱环境中运行，防止产生死循环或恶意破坏。

### 防线二：设计"黄金安全看守"（Human-in-the-Loop）

在多步链路中，设置明确的人工介入点（Guardrails）：
- **决策看守**：当Agent动态规划的步骤中出现"关键决策点"（如判断一个退款申请是否合理）时，系统必须暂停，等待人类审核批准后，Agent才能继续往下执行。
- **输出看守**：在最终把结果写入数据库、发送给客户、或归档发布前，必须由人工在系统界面上核对、修改并确认发出。

### 防线三：状态机约束（State Machine Constraint）

不要让Agent完全自由地决定下一步做什么。使用传统软件的**状态机（State Machine）**对 Agent 进行轨道约束：
- 预先定义好 Agent 只能在 $A 	o B 	o C$ 或 $A 	o C 	o D$ 这几种合法的状态路径中跳转。
- 模型只负责在当前状态下，根据输入选择"跳转到哪个合法的下一步"，而不是自己发明一个新的步骤。这能将 Agent 的行为范围框在一条设计好的"轨道"上，避免其迷路。

---

## 五、Agent的上线治理检查表

在允许一个Agent进入试运行前，必须通过以下8项治理核对：

- [ ] **1. 角色与责任人**：明确指定了该Agent的业务责任人（谁对它的输出质量负责）和技术责任人（出 bug 谁来修）。
- [ ] **2. 自主等级定义**：明确定义了该Agent的自主等级（如 L3 还是 L4），并在系统架构上实现了物理限制。
- [ ] **3. 限制写权限**：验证了Agent无法通过任何调用链对核心系统进行未经审核的"写"操作。
- [ ] **4. 自动熔断机制**：设置了最大Loop限制（例如单个任务最多调用 LLM 8 次，超过则自动强行中止并报警），防止陷入无限调用黑洞产生巨额账单。
- [ ] **5. 人工确认节点**：在最终执行输出和关键决策点，设置了必须人类一键确认的界面。
- [ ] **6. 评测集就绪**：准备了包含至少20个真实历史任务的评测集，且Agent在该评测集上的测试通过率 ≥ 85%。
- [ ] **7. 异常处理机制**：当调用API超时、LLM返回格式错误、或触发熔断时，系统有明确的"退回到纯人工操作"的平滑回退路径。
- [ ] **8. 审计日志**：系统会完整记录 Agent 的每一步规划、调用了什么工具、LLM 的输入输出原文，便于事后追溯和 debug。

---

## 六、本章行动清单

**本周立即执行：**
- [ ] 盘点你们团队目前正在开发或计划开发的 Agent/复杂自动化项目。
- [ ] 用本章的**适用条件决策树**逐一重新评估，判断是否有场景"被过度设计成了 Agent"，并降级为单步工作流或确定性流水线。
- [ ] 为所有在研的 Agent 明确指定它的自主等级（L1~L5）。

**下个月内建立：**
- [ ] 检查正在运行的自动化流程，是否设置了"最大循环限制"（熔断阀门），若没有立即补上。
- [ ] 确保所有自动化输出在到达真实客户或核心系统前，都有一个"人工看守"的界面。

---

## 本章小结

Agent 不是魔法，它是一个大语言模型与外部工具交互的概率系统。

自主性是一把双刃剑：它提升了处理复杂、动态任务的能力，但也带来了成功率的乘法衰减和错误传播。

在企业级应用中，优秀的治理者始终保持克制：能用单步工作流就不用多步；能用确定性状态机约束就不用完全自主 Agent；永远在关键输出关口保留人工看守；并且随时准备好平滑的回退路径。

把控制权抓在手里，才是系统长期可靠的唯一保证。
